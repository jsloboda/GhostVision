for i = 1

    figure(eval(i));

    subplot(3,1,1);
    name1 = sprintf('data%d', i);
    plot(eval(name));

    subplot(3,1,2);
    name2 = sprintf('data%df', i);
    plot(eval(name2));

    subplot(3,1,3);
    name3 = sprintf('data%d_pulse', i);
    plot(eval(name3));

end